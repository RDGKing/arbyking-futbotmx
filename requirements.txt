# =============================================================================
# Copa FutBotMX — ArbyKing (notebooks/arby-futbot.mx.ipynb)
# =============================================================================
#
# GOOGLE COLAB — instala solo esto (Colab ya trae torch, jupyter, etc.):
#
#   pip install ultralytics supervision opencv-python-headless matplotlib numpy
#
# ENTORNO LOCAL LIMPIO (venv/conda sin nada preinstalado):
#
#   pip install -r requirements.txt
#
# =============================================================================
# Imports directos del notebook
# =============================================================================
#   cv2                          → opencv-python
#   matplotlib.pyplot            → matplotlib
#   numpy                        → numpy
#   supervision                  → supervision  (ByteTrack, anotadores)
#   ultralytics (SAM, YOLO)      → ultralytics
#   pathlib                      → stdlib (no instalar)
#
# =============================================================================

# --- Paquetes que importa el notebook ---
ultralytics>=8.4.0
supervision>=0.28.0
matplotlib>=3.8.0
numpy>=1.24.0
opencv-python>=4.8.0

# --- Lo que Colab ya trae, pero hace falta en un entorno local limpio ---
# ultralytics los usa por dentro (YOLO train, SAM inferencia, leer futbot.yaml)
torch>=2.0.0
torchvision>=0.15.0
pillow>=10.0.0
pyyaml>=6.0
scipy>=1.10.0
pandas>=2.0.0
tqdm>=4.65.0

# --- Opcional: abrir y correr el .ipynb en tu PC ---
# jupyter>=1.0.0
# ipykernel>=6.0.0
